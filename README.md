## Just a Roblox Arsenal Triggerbot



### build and run
I don't know how windows works so I built it using wsl; Read the build script I guess.
If you are using wsl, just run `./b` or make.

### how?
It uses windows.h to scan next to the crosshair and clicks if it sees red(, this is way far consistent when you're on blu team with red enemy outlines)

### TLDR
This isn't exactly detectable unless you're screensharing. Also note that this can't be detected by the anti cheat since this is a fully external program. The horrid networking that Roblox uses is terrible, this means that this triggerbot will just look like the normal ping advantage; I'm not concerned that this could cause any real effect since it's so shit anyway, but it does work.
